*** .config	2014-02-26 21:11:44.394541736 +0100
--- .config	2014-02-26 21:14:13.356691376 +0100
***************
*** 9,14 ****
--- 9,16 ----
  # be modified from here. In most cases, these lines should use += in order not
  # to override previous values of the variables.
  
+ CFLAGS += -I/home/qafir/linux/env/usr/include
+ LIBS += -L/home/qafir/linux/env/usr/lib -L/home/qafir/linux/env/usr/lib64
  
  # Uncomment following two lines and fix the paths if you have installed OpenSSL
  # or GnuTLS in non-default location
***************
*** 34,47 ****
  # driver_nl80211.c requires libnl. If you are compiling it yourself
  # you may need to point hostapd to your version of libnl.
  #
! #CFLAGS += -I$<path to libnl include files>
! #LIBS += -L$<path to libnl library files>
  
  # Use libnl v2.0 (or 3.0) libraries.
  #CONFIG_LIBNL20=y
  
  # Use libnl 3.2 libraries (if this is selected, CONFIG_LIBNL20 is ignored)
! #CONFIG_LIBNL32=y
  
  
  # Driver interface for FreeBSD net80211 layer (e.g., Atheros driver)
--- 36,49 ----
  # driver_nl80211.c requires libnl. If you are compiling it yourself
  # you may need to point hostapd to your version of libnl.
  #
! CFLAGS += -I/home/qafir/linux/env/usr/include/libnl3
! LIBS += -L/home/qafir/linux/env/usr/lib
  
  # Use libnl v2.0 (or 3.0) libraries.
  #CONFIG_LIBNL20=y
  
  # Use libnl 3.2 libraries (if this is selected, CONFIG_LIBNL20 is ignored)
! CONFIG_LIBNL32=y
  
  
  # Driver interface for FreeBSD net80211 layer (e.g., Atheros driver)
***************
*** 151,157 ****
  #CONFIG_EAP_TNC=y
  
  # Wi-Fi Protected Setup (WPS)
! #CONFIG_WPS=y
  # Enable WSC 2.0 support
  #CONFIG_WPS2=y
  # Enable WPS external registrar functionality
--- 153,159 ----
  #CONFIG_EAP_TNC=y
  
  # Wi-Fi Protected Setup (WPS)
! CONFIG_WPS=y
  # Enable WSC 2.0 support
  #CONFIG_WPS2=y
  # Enable WPS external registrar functionality
***************
*** 213,219 ****
  # This can be used to reduce the size of the wpa_supplicant considerably
  # if debugging code is not needed. The size reduction can be around 35%
  # (e.g., 90 kB).
! #CONFIG_NO_STDOUT_DEBUG=y
  
  # Remove WPA support, e.g., for wired-only IEEE 802.1X supplicant, to save
  # 35-50 kB in code size.
--- 215,221 ----
  # This can be used to reduce the size of the wpa_supplicant considerably
  # if debugging code is not needed. The size reduction can be around 35%
  # (e.g., 90 kB).
! CONFIG_NO_STDOUT_DEBUG=y
  
  # Remove WPA support, e.g., for wired-only IEEE 802.1X supplicant, to save
  # 35-50 kB in code size.
***************
*** 468,474 ****
  # P2P (Wi-Fi Direct)
  # This can be used to enable P2P support in wpa_supplicant. See README-P2P for
  # more information on P2P operations.
! #CONFIG_P2P=y
  
  # Enable TDLS support
  #CONFIG_TDLS=y
--- 470,476 ----
  # P2P (Wi-Fi Direct)
  # This can be used to enable P2P support in wpa_supplicant. See README-P2P for
  # more information on P2P operations.
! CONFIG_P2P=y
  
  # Enable TDLS support
  #CONFIG_TDLS=y
***************
*** 476,482 ****
  # Wi-Fi Direct
  # This can be used to enable Wi-Fi Direct extensions for P2P using an external
  # program to control the additional information exchanges in the messages.
! #CONFIG_WIFI_DISPLAY=y
  
  # Autoscan
  # This can be used to enable automatic scan support in wpa_supplicant.
--- 478,484 ----
  # Wi-Fi Direct
  # This can be used to enable Wi-Fi Direct extensions for P2P using an external
  # program to control the additional information exchanges in the messages.
! CONFIG_WIFI_DISPLAY=y
  
  # Autoscan
  # This can be used to enable automatic scan support in wpa_supplicant.
*** Makefile	2014-02-04 12:23:35.000000000 +0100
--- Makefile	2014-02-26 21:19:56.187477219 +0100
***************
*** 6,13 ****
  CFLAGS = -MMD -O2 -Wall -g
  endif
  
! export LIBDIR ?= /usr/local/lib/
! export BINDIR ?= /usr/local/sbin/
  PKG_CONFIG ?= pkg-config
  
  CFLAGS += -I$(abspath ../src)
--- 6,13 ----
  CFLAGS = -MMD -O2 -Wall -g
  endif
  
! export LIBDIR ?= /usr/lib/
! export BINDIR ?= /usr/sbin/
  PKG_CONFIG ?= pkg-config
  
  CFLAGS += -I$(abspath ../src)
